[project]
name = "yuyan"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.11"
dependencies = [
    "polars[rtcompat]>=1.41.2",
    "pyarrow>=24.0.0",
    "yfinance>=1.4.1",
]

[dependency-groups]
dev = [
    "ipykernel>=7.2.0",
    "nbformat>=5.10.4",
    "plotly>=6.8.0",
]
