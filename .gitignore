# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Record datastore (data/records + data/blobs) is committed.
# Pre-refactor data lives in data_old/ (large, untracked, reproducible) and is not committed.
data_old/

# Local Claude Code settings
.claude/settings.local.json
